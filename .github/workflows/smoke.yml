name: Smoke

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[dev]
      - name: Run public demo smoke flow
        run: |
          bash scripts/demo_public_flow.sh .ci-demo-output
      - name: Run smoke tests
        run: |
          pytest \
            tests/test_import_smoke.py \
            tests/test_schema_contracts.py \
            tests/test_demo_assets.py \
            tests/test_trace_store.py \
            tests/test_cli_run_execute.py \
            tests/test_cli_run_score.py \
            tests/test_cli_run_export.py \
            tests/test_cli_dataset.py
